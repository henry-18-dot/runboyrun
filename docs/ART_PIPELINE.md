# Art Pipeline (Placeholder)

1. Produce sprite sheets for rider/horse states in `assets/sprites/rider_horse/`.
2. Update `assets/data/sprite_manifest.json` and `rider_horse_manifest.json`.
3. Switch `SpriteAnimator` implementation when assets are ready.
